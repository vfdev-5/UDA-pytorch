name: uda_env
channels:
  - defaults
  - pytorch
dependencies:
  - python=3.7
  - numpy
  - pytorch>=1.1.0
  - torchvision
  - pip
  - pip:
    - mlflow
    - tqdm
    - tensorboardX
    - git+https://github.com/pytorch/ignite.git
